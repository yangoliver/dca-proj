# P1 - Day 4 参考指南：PE 估值理解 + 弹性股数法 + 写出定投工具

> **核心目标**：理解 PE 和 PE 分位；掌握弹性股数法；用 Qoder CN 写出第一版定投工具；把 Day 1 第一次购买记录和余额补充进 data/portfolio.xlsx；为下一次定投做好全部准备。
>
> **双周定投**：每 **14 天**一次，全程 20 次约 9 个月（2026-07-20 → 2027-04-12）。

---

## 一、今天任务清单

### 必做任务（今天必须完成）

| # | 任务 | 完成标准 |
|---|------|---------|
| 1 | **读懂 PE 比率** | 能用自己的话解释"510580 的 PE 是 26 倍"是什么意思 |
| 2 | **读懂 PE 分位** | 能解释"PE 分位 60%"的含义，以及它和"便宜/贵"的关系 |
| 3 | **写出定投工具 v1.0** | 用 Qoder CN 写出 7 个文件（详见第八章规范） |
| 4 | **Day 1 记录进 Excel** | data/portfolio.xlsx 填入 Day 1 第一次购买日期、价格、手数、金额、余额 |
| 5 | **初始化定投日历** | 程序选5，Sheet3 生成全部20次双周计划 |
| 6 | **工具自测验证** | 程序选 1/2/3/4/5 全部跑通 |
| 7 | **Day 4 报告** | 填写并提交到 GitHub |

> **注意**：今天不操作中信证券 APP，不做真实买入。工具写好、跑通、Excel 填好之后，下一次买入日直接用程序选1，不需要问人。

### 选做任务（加分）

| # | 任务 |
|---|------|
| 7 | 用 Qoder CN 画一张 PE 历史走势图（近 5 年） |
| 8 | 把 PE 分位数据画成条形图，直观看高低 |

---

## 二、什么是 PE（从零讲透）

### 2.1 先理解"回本"的概念

想象你开了一家店，花了 **10 万元**。

- 每年净赚 **2 万元**
- **几年能回本？** → 10 ÷ 2 = **5 年**

这 5 年，就是你的"回本年限"。

---

### 2.2 PE = 回本年限

**PE = Price / Earnings**，即**股价 ÷ 每股收益**。

> **如果以现在的价格买下整个公司（指数），需要多少年的利润才能回本？**

| 例子 | 股价 | 每年盈利 | PE = 回本年限 |
|------|------|---------|-------------|
| 稳健型股票 | ¥10 | ¥1.0 | 10 年 |
| 成长型股票 | ¥30 | ¥1.0 | 30 年 |
| 周期低谷股 | ¥5 | ¥0.5 | 10 年 |

**关键规律**：
- PE **越高** → 回本越慢 → 估值**越贵**
- PE **越低** → 回本越快 → 估值**越便宜**

---

### 2.3 中证500 当前 PE 约 26 倍是什么意思？

> "中证500 的 PE 是 26 倍" = 以今天的价格买中证500，需要约 26 年的指数利润才能回本。

- 历史平均 PE 约 25-30 倍 → 今天接近平均水平
- 2015 年泡沫期 PE 曾到 80-100 倍 → 今天比那时候便宜多了
- 2018 年底部 PE 约 18 倍 → 今天比那时候贵一些

---

### 2.4 为什么 PE 会变？（反直觉规律）

PE 变化的两个驱动因素：**股价**和**每股收益（EPS）**都在变。

| 市场情况 | 股价 | EPS（盈利） | PE 怎么变 |
|---------|------|-----------|---------|
| 暴跌（危机） | 大跌 | EPS 也跌（企业亏损） | PE 可能**上升**（盈利跌得更快） |
| 大涨（牛市） | 大涨 | EPS 增长（企业赚钱） | PE 可能**不变或下降** |
| 震荡筑底 | 不变 | EPS 增长 | PE **下降**（盈利在改善）|

> PE 是**结果**，不是**原因**。不要根据 PE 高低直接决策。

---

## 三、什么是 PE 分位（关键概念）

### 3.1 "分位"就是排名

想象全班 100 名同学考试：

- 小明考了第 10 名 → **前 10%**（比 90% 的人分数高）
- 小红考了第 90 名 → **后 10%**（比 90% 的人分数低）

PE 分位就是这个意思：

> **今天的 PE，在历史所有 PE 数据中，排第几名？**

---

### 3.2 PE 分位的含义

**PE 近 5 年分位 = 60%** 的意思是：

> 过去 5 年里，有 60% 的时间，PE **比今天低**；
> 有 40% 的时间，PE **比今天高**。

---

### 3.3 分位与"贵/便宜"的关系

| PE 分位 | 含义 | 市场状态 |
|---------|------|---------|
| **0%~20%** | 历史最低区间 | 🔥 极度便宜 |
| **20%~40%** | 低于历史平均 | ✅ 比较便宜 |
| **40%~60%** | 接近历史平均 | ➡️ 正常估值 |
| **60%~80%** | 高于历史平均 | ⚠️ 偏贵，留意止盈 |
| **80%~100%** | 历史最高区间 | 🔴 极度昂贵 |

> **本项目止盈提醒线**：PE 近 5 年分位 > 70% → QClaw 提醒你关注止盈

---

### 3.4 中证500 历史 PE 对照（建立体感）

| 日期 | 中证500 点位 | 估计 PE | 市场背景 |
|------|------------|--------|---------|
| 2015-06 | 11,546 | ~80-100倍 | 历史大顶，泡沫 |
| 2018-10 | 4,268 | ~18倍 | 历史大底 |
| 2020-03 | 5,600 | ~25倍 | COVID 恐慌底 |
| 2024-02 | 5,400 | ~22倍 | AI 行情前，极度便宜 |
| 2025-06 | 7,200+ | ~38倍 | AI 行情顶端 |
| 今天 | 约5,400 | ~26倍 | 回到历史均值附近 |

---

### 3.5 为什么中证500 的 PE 分位会"失真"？

**PE = P ÷ E，P 是股价，E 是盈利**

中证500 成分股以中小盘为主，盈利波动大：

```
情况 A：盈利上行期（牛市）
→ E（盈利）快速变大
→ 分母变大 → PE 被"被动压低"
→ 看起来"便宜"，其实是高点

情况 B：盈利底部期（衰退）
→ E 变小甚至变负
→ 分母变小 → PE 被"被动推高"
→ 看起来"很贵"，其实是底部
```

**结论**：
- PE 分位**不能**直接触发买卖操作
- 它只能告诉你：**现在是历史高位还是低位，作为参考**
- 真正的止盈触发条件是**目标收益率（+25%~30%）**，不是分位数

---

## 四、弹性股数法（解决 100 手数约束）

### 4.1 问题

A股 ETF 必须**整手交易**，1 手 = **100 份**。

510580 约 **¥4.0/份**：
- 1 手 = 100 份 = **¥400**
- ¥500 → 只能买 1 手，**剩 ¥100**

¥5.5/份时，1 手 = ¥550 → **¥500 买不了 1 手**

---

### 4.2 弹性股数法（方案 A）—— 每次重新算

**计算公式**：
```
最大可买手数 = floor(500 ÷ 当前价格 ÷ 100)
实际买入份额 = 最大可买手数 × 100
实际花费    = 实际买入份额 × 当前价格
滚存余额    = 500 - 实际花费
```

**例子**（¥4.0/份）：
```
floor(500 ÷ 4.0 ÷ 100) = floor(1.25) = 1 手
→ 买 100 份，花 ¥400，剩 ¥100 滚存
```

**例子**（¥5.5/份）：
```
floor(500 ÷ 5.5 ÷ 100) = floor(0.909) = 0 手
→ 买不了 1 手，本次 ¥500 全部滚存到下次
```

---

## 五、完整今天流程

**正确的顺序**：

| 步骤 | 做什么 | 产出 |
|------|--------|------|
| 1 | 先读第二章（PE 理解）| 建立概念 |
| 2 | 用 Qoder CN 写程序（分步）| tools/ 下 7 个文件 |
| 3 | 安装依赖，跑通 main.py | 工具跑通 |
| 4 | 程序选 5（初始化日历）| Sheet3 有全部20次计划 |
| 5 | 程序选 1（模拟定投）| 验证买入计算正确，Sheet3标记✅ |
| 6 | 程序选 2/3/4 | 验证持仓/历史/PE 查询正常 |
| 7 | 写 Day 4 报告，提交 PR | GitHub |

**今天结束后工具状态**：程序写好跑通，data/portfolio.xlsx Sheet3 有全部20次计划（第1次：2026-07-20 已完成），随时可以执行第二次买入。

---

## 六、核心记忆点（背下来）

| 记忆点 | 内容 |
|--------|------|
| PE 是什么 | 股价 ÷ 每股收益 = "几年回本" |
| PE 越低 | 越便宜（回本快） |
| PE 越高 | 越贵（回本慢） |
| PE 分位是什么 | 今天 PE 在历史 PE 中的排名百分比 |
| PE 分位 60% 含义 | 比 60% 的历史时间便宜，比 40% 的时间贵 |
| 中证500 PE 分位会失真 | 中小盘盈利波动大，PE 被盈利被动推拉 |
| 分位 >70% | QClaw 提醒关注止盈，但触发止盈的是目标收益率 |
| 弹性股数法 | `floor(500 ÷ 价格 ÷ 100)` 手，每次重新算 |
| 滚存余额 | 每次买完剩余的钱，留账户里累积到下次 |

---

## 七、思考题

### 思考题 1：PE 的陷阱
> 2015 年 6 月，中证500 PE 高达 80-100 倍，但指数点位是历史最高。
> 2024 年 2 月，PE 只有 22 倍，指数约 5400 点——并不是历史最低点位。
> **为什么 PE 最低时，点位却不是历史最低？**
（提示：PE = P/E，P 和 E 都在变）

### 思考题 2：分位的悖论
> 如果 PE 分位是 0%（历史最低），意味着市场很便宜。
> 但如果之后继续下跌，PE 分位反而可能**上升**。
> **这说明"便宜"和"继续涨"是什么关系？**

### 思考题 3：弹性股数法的代价
> 弹性股数法导致每次投入 ¥400~¥500 不等。
> 长期来看，这种微小差异对最终收益影响大吗？
> （提示：¥400 和 ¥500 的差距，占 ¥10,000 总投入的百分比是多少？）

---

## 八、你今天的编程任务：写出定投工具 v1.0

> **这是 Day 4 最重要的任务。程序写完，以后每次定投都用它——算买入、记 Excel、查持仓，全部自动化。**

### 8.1 这个工具做什么

每次定投时：
1. **自动算出该买多少**（弹性股数法）
2. **自动记录到 Excel**（不用手写）
3. **随时查看持仓**（成本/市值/盈亏）
4. **查 PE 估值**（akshare 自动拉）
5. **查看定投日历**（20次计划，标记已完成/待执行）

---

### 8.2 文件结构

在项目根目录 `tools/` 下创建（不是 Day4/ 下）：

```
tools/
├── main.py          # 主入口（python main.py 运行）
├── config.py        # 配置：只改这5行，其他不动
├── calculator.py    # 弹性股数法
├── recorder.py      # Excel 读写（持仓记录 + 定投日历）
├── portfolio.py     # 持仓分析 + PE估值查询
├── scheduler.py     # 定投日历生成（20次计划）
├── requirements.txt # 依赖
└── data/portfolio.xlsx   # 持仓记录（Excel，打开直接看）
```

---

### 8.3 模块设计规范

---

#### `config.py` — 只改这里

```python
# ========== 只改这5行 ==========
ETF_CODE      = "510580"        # 易方达中证500ETF
ETF_NAME      = "易方达中证500ETF"
INVEST_AMOUNT = 500            # 每次定投金额（元）
FIRST_DATE    = "2026-07-20"  # 第一次买入日期
TOTAL_COUNT   = 20             # 总共计划定投次数（20次）
# ===================================
```

---

#### `calculator.py` — 弹性股数法

```python
def calc_lots(amount: float, price: float) -> dict:
    """
    弹性股数法：计算本次能买多少
    返回: dict(lots=1, shares=100, spent=420.0, leftover=80.0)
    """
    lot_size = 100
    max_lots = int(amount // (price * lot_size))
    shares   = max_lots * lot_size
    spent    = round(shares * price, 2)
    leftover = round(amount - spent, 2)
    return dict(lots=max_lots, shares=shares, spent=spent, leftover=leftover)
```

---

#### `recorder.py` — Excel 读写

```python
def add_purchase(date: str, price: float, shares: int,
                 spent: float, leftover: float, note: str = ""):
    """追加一条买入记录到 data/portfolio.xlsx"""

def get_all_records() -> list[dict]:
    """返回所有历史买入记录：[{"date":..., "price":..., "shares":..., "spent":...}, ...]"""
```

**Excel `data/portfolio.xlsx` 结构**：
- Sheet1 `持仓记录`：日期、价格、份额、花费、滚存、备注
- Sheet2 `累计滚存`：日期、本次滚存、累计滚存
- Sheet3 `定投日历`：计划日期（第1~20次）、实际执行日期、执行状态（✅已完成/⏳待执行）

---

#### `portfolio.py` — 持仓分析 + PE 估值

```python
def analyze() -> dict:
    """
    计算持仓状态：
    total_shares, total_invested, avg_cost,
    current_price(akshare), current_value, profit, profit_pct
    """

def get_pe_data() -> dict:
    """
    用 akshare 获取中证500当前估值：
    PE(TTM), PB, 近5年分位, 近5年最高/最低PE
    """
```

**平均成本算法**：
```
avg_cost = 总投入金额 ÷ 总份额

第1次：¥410买100份，第2次：¥425买100份
avg_cost = (410+425) ÷ (100+100) = ¥4.175/份
```

---

#### `scheduler.py` — 定投日历

```python
def generate_schedule(first_date: str, count: int) -> list[dict]:
    """
    生成20次定投计划日历
    返回: [{"no": 1, "planned": "2026-07-20", "actual": "2026-07-20", "status": "✅已完成"},
           {"no": 2, "planned": "2026-08-03", "actual": "", "status": "⏳待执行"}, ...]
    """

def init_schedule(first_date: str, count: int):
    """生成全部20次计划，写入 data/portfolio.xlsx 的 Sheet3"""

def mark_done(no: int, actual_date: str):
    """标记第N次已完成"""
```

运行 `python main.py` 显示菜单：

```
╔══════════════════════════════════════╗
║     OPC 基金定投工具 v1.0           ║
╚══════════════════════════════════════╝
今日是第 N 次定投（上次：YYYY-MM-DD）
下次定投日：YYYY-MM-DD

  [1] 🟢 今天执行定投（查价→计算→记录）
  [2] 📊 查看持仓状态
  [3] 📜 查看历史记录
  [4] 📈 查看 PE 估值数据
  [5] 📅 查看定投日历（20次计划）

> _
```

**选 1 流程**：akshare拉价 → 显示当前价 → 弹性股数法算结果 → 确认买入 → 写入Excel（Sheet1持仓记录+Sheet3日历标记✅） → 显示持仓概览

---

### 8.4 怎么问 QClaw 写代码（分步来）

**第 1 步：config.py + calculator.py**
```
请帮我写两个文件：
1. config.py：4行配置（ETF代码、名称、定投金额、起始日）
2. calculator.py：弹性股数法函数，输入金额和价格，返回买几手、买多少份、花多少钱、滚存多少
Python，中文注释。
```

**第 2 步：recorder.py（最难，先问）**
```
请帮我写 recorder.py，实现 Excel 读写：
1. add_purchase()：把一行数据追加到 data/portfolio.xlsx 的 Sheet1（自动创建文件+写表头）
2. get_all_records()：读取 Sheet1 所有行，返回 list[dict]
用 openpyxl 库。
```

**第 3 步：portfolio.py（持仓分析+PE查询）**
```
请帮我写 portfolio.py：
1. analyze()：用 recorder.py 的 get_all_records() 获取历史数据，计算总份额、总投入、平均成本
2. 用 akshare 的 stock_zh_a_spot_em() 获取当前价格，显示市值和浮盈浮亏
3. get_pe_data()：用 akshare 获取中证500指数（000905）的当前 PE、PB、近5年分位、近5年最高/最低PE
   （用你能想到的 akshare 方法尝试）
```

**第 4 步：main.py（菜单入口）**
```
请帮我写 main.py：
主循环显示菜单，选项：
1. 执行定投（akshare查价→calculator算→recorder写Excel→portfolio显示持仓→scheduler标记✅）
2. 查看持仓（调用 portfolio.analyze()）
3. 查看历史记录（调用 recorder.get_all_records()）
4. 查看 PE 估值（调用 portfolio.get_pe_data()）
5. 查看定投日历（调用 scheduler 显示全部20次双周计划，第1次2026-07-20，第20次2027-04-12）
调用 config.py 读取配置。
```

**第 5 步：scheduler.py（定投日历）— 必做作业 ⚠️**

```
用 Qoder CN 帮我写 scheduler.py，实现以下三个函数：

1. generate_schedule(first_date, count)：
   从 first_date 开始，每 14 天一次，生成 count 次计划列表
   返回 [{'no':1,'planned':'2026-07-20','actual':'','status':'⏳待执行'}, ...]

2. init_schedule(first_date, count)：
   调用 generate_schedule，生成全部计划，写入 data/portfolio.xlsx Sheet3
   （如果 Sheet3 不存在则自动创建）

3. mark_done(no, actual_date)：
   把第 N 次的 actual_date 和 status 更新为"已完成 ✅"

config.py 里有 FIRST_DATE 和 TOTAL_COUNT，导入后直接用。

⚠️ 注意：两次定投间隔 14 天（双周），不是 7 天。
第1次：2026-07-20，第2次：2026-08-03，第3次：2026-08-17，以此类推。
```

**第 6 步：测试**
```
python main.py
选 5 → 确认显示全部20次计划（第1次：2026-07-20，第20次：2027-04-12）
选 3 → 确认能读取历史记录
选 1 → 确认 akshare 能查到价格，Excel 写入成功（Sheet1+Sheet3标记）
选 2 → 确认持仓概览正确
选 4 → 确认 PE 数据展示正常
```

---

### 8.5 requirements.txt

```
openpyxl>=3.1.0
akshare>=1.14.0
```

安装：`pip install openpyxl akshare`

---

### 8.6 验收标准

```
☐ python main.py 显示菜单（含选5）
☐ 选 1：akshare 能查到 510580 价格
☐ 弹性股数法计算正确（floor(500/价格/100)）
☐ 选 1 确认后 data/portfolio.xlsx Sheet1 有新行
☐ 选 1 执行后 data/portfolio.xlsx Sheet3 自动标记第N次为✅已完成
☐ 选 2：显示累计份额、平均成本、市值、浮盈浮亏
☐ 选 3：列出所有历史记录
☐ 选 4：显示当前 PE/PB/分位
☐ 选 5：显示完整20次定投日历（Sheet3内容）
☐ data/portfolio.xlsx Sheet3 有全部20次计划日期（第1次：2026-07-20）
☐ config.py 改 ETF 代码后，其他文件不受影响
☐ 今天不做真实买入（工具准备好即可）
```

---

### 8.7 提交 GitHub

```bash
cd ~/ws/dca-proj
git add tools/
git commit -m "feat(Day4): 定投工具v1.0"
git push
# 提 PR
```

**data/portfolio.xlsx 每次买入后提交 GitHub**：

`data/portfolio.xlsx` 是真实持仓记录，每次买入后程序自动更新，**同时提交到 GitHub**。

```bash
git add tools/
git commit -m "chore: update data/portfolio.xlsx"
git push
```

---

**Day 4 的核心目标**：理解 PE 和分位；掌握弹性股数法；用 Qoder CN 写出定投工具（含 scheduler.py）；把 Day 1 第一次购买记录和余额补充进 data/portfolio.xlsx；程序管计算和记录，Excel 是唯一真实数据来源；下一次买入日直接用程序，不需要问人。

Good luck! 🚀
