# Day 11 参考指南 — 补齐三个规则漏洞

> **核心目标**：Day 1–Day 10 把系统跑起来了，但有几个规则没有定义清楚。今天补上三个核心规则漏洞：
> 1. 1 万元够不够、要不要追加（先做）
> 2. PE < 30% 的止盈资金加回机制
> 3. Cron 巡检频度
>
> 今天主要写代码——把分析工具写出来，用数字回答问题，把规则改到位。

---

## 一、1 万元够不够（先做）

### 1.1 先写一个分析工具

在 `tools/` 下新建 `smile_curve_analyzer.py`。

这个工具接收两个参数：

- ETF 代码（如 `510580`）
- 总投资金额（如 `10000`）

输出：

- 历史最大跌幅 + 发生时间
- 完整微笑曲线需要多久、总投入多少
- 两个建议方案：
  - **方案 A**：降低每期金额，延长周期覆盖微笑曲线
  - **方案 B**：保持每期金额，追加总投资

**工具设计要求**：

```python
def analyze_investment_plan(etf_code, total_amount, interval_days=14, per_amount=500):
    """
    分析给定总投资金额能否覆盖一条完整微笑曲线。

    返回：
    - 最大跌幅（%）
    - 完整微笑曲线所需月数
    - 完整微笑曲线所需总投入
    - 方案A：降低每期金额后的月数 + 每期金额
    - 方案B：追加金额后需追加多少
    """
```

**分析逻辑**：

1. akshare 拉取 ETF 过去 5 年历史数据
2. 找到跌幅最深的一段（最大回撤）
3. 模拟定投：每 14 天投 ¥500，算出覆盖这段微笑曲线需要多少期、共投入多少
4. 用总金额和所需投入对比，判断够不够
5. 如果不够，给出方案 A（减金额延周期）和方案 B（追加金额）

### 1.2 用 510580 + 1 万元跑一遍

运行工具，输出结果。

### 1.3 把分析结论写进报告

| # | 问题 | 答案 |
|---|------|------|
| 1 | 历史最大跌幅多少？ | （%，发生在___） |
| 2 | 完整微笑曲线需要多少期、共投入多少？ | （___ 期，¥___） |
| 3 | 1 万元够覆盖吗？ | （够 / 不够） |
| 4 | 方案 A：降低每期到多少元，可以覆盖？ | （¥___/期，___ 期） |
| 5 | 方案 B：追加多少元，可以覆盖？ | （需追加 ¥___） |
| 6 | **你的建议**：现有计划怎么调整？ | （你选的方案 + 理由） |

### 1.4 自测

用另一个 ETF（如 510300 沪深300ETF）跑一遍，确认工具通用。

---

## 二、PE < 30% 止盈资金加回机制

### 2.1 先读 Day 6–9 的历史记录

打开 Day 6 报告（第 13、31、64 页）和 Day 9 核对材料（方案 B·③ 持有），找到现在 pe_low 的提示词。

### 2.2 两条策略，选一条

PE < 30% 时，货基里的止盈资金怎么处理？两条路，性质完全不同。

---

**策略 A：保守型——止盈资金不回来**

| 规则 | 内容 |
|------|------|
| 性质 | 止盈资金是"多赚的防守资金"，不是仓位 |
| PE < 30% 时 | inspector 只提醒"市场低估"，不提加仓 |
| 货基里的钱 | 长期放着，不回到定投系统 |
| inspector pe_low 提示词 | "市场处于低估区间（PE分位 < 30%）。你的止盈资金在货基里，按策略 A 不做额外操作，继续正常定投。" |

**优点**：规则极简，不需要判断时机，没有"钱回来了怎么买"的问题。
**缺点**：止盈资金没有复利。

---

**策略 B：机会型——低位额外加仓**

| 规则 | 内容 |
|------|------|
| 性质 | 止盈资金是"低位加仓的子弹" |
| PE < 30% 时 | inspector 提醒：当前分位 __%，货基余额 ¥__，可额外加仓 |
| 每次加仓上限 | 不超过 ¥500，也不超过货基余额的 50% |
| 执行方式 | inspector 提醒 → 你判断 → 发指令给 Oliver |
| 停止提醒条件 | PE 回到 30% 以上，或货基余额 = ¥0 |
| inspector pe_low 提示词 | "市场处于低估区间（PE分位 < 30%）。你有 ¥__ 在货基里，可考虑额外加仓 ¥__（不超过 ¥500）。是否加仓、由你判断。" |

**优点**：止盈资金在低位重新进入市场，有机会赚更多。
**缺点**：规则更复杂，需要人在回路，容易执行不到位。

---

**选完后**，把 pe_low 提示词更新为对应策略的内容，写进报告。

### 2.3 自测

- PE 分位 = 28% + 货基余额 ¥300（策略 A）→ inspector 输出？
- PE 分位 = 28% + 货基余额 ¥300（策略 B）→ inspector 输出？
- PE 分位 = 32% + 货基余额 ¥300 → inspector 输出？

---

## 三、Cron 巡检频度

### 3.1 查现状

运行 `cron list`，确认现有 Cron 状态。

### 3.2 重新设计

推翻月度巡检，改用**每日轻量巡检 + 有情况才推**。

触发推送的条件：

| 条件 | 动作 |
|------|------|
| 浮盈 ≥ 20%（接近止盈） | 推送：接近止盈线，注意观察 |
| 浮亏 ≥ 15% | 推送：浮亏预警，检查是否偏离纪律 |
| PE 分位 < 30% | 推送：低估区间，止盈资金可考虑额外加仓 |
| PE 分位 > 70% | 推送：估值偏高，留意止盈机会 |
| 距下次定投日 ≤ 2 天 | 推送：定投提醒（金额、手数） |
| 无以上情况 | Cron 静默，不打扰 |

### 3.3 修改 inspector.py

新增"无情况静默"模式，更新每日巡检 Cron。

---

## 四、Day 11 报告

打开 `Day11/report/justin-Day11报告.md`，填写：

1. **1 万元够不够**：smile_curve_analyzer.py 分析结果 + 方案 A/B + 你的建议
2. **PE < 30% 加回机制**：四条规则 + inspector.py 修改
3. **Cron 频度**：新设计逻辑

提交到 GitHub。
