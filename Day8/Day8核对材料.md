# Day 8 核对材料 — 缺口核对与参考改法（独立核对材料）

> ⚠️ **打开时机**：请先完成《Day 8 参考指南》的 Step A（用 AI 工具自查）、并写出**你自己的**缺口清单与修复方案，再来核对本文件。本文件是"参考答案"，不是"作业题面"——提前看会剥夺你自己发现的机会。
>
> 本文件分两部分：
> - **第一部分·缺口核对表**：我量出来的 `tools/` 缺口清单，按 Day 8 的两份方案分两块——**方案 A（五步↔工具增强）缺口**（①~④ 步每步工具差在哪、散落资产怎么归位）、**方案 B（自动化增强）缺口**（⑤ 步"定时提醒/周期巡检"差在哪）。对照时请按"方案A缺口 / 方案B缺口"逐块核对。
> - **第二部分·参考改法**：我对每个缺口的参考实现思路（Day 9 动手前打开），供你核对"你打算的改法是否覆盖、有无更好的写法"。
>
> 允许你采纳、改进、或提出完全不同的改法——只要最终结果对齐第三课口径。

---

## 第一部分·缺口核对表

我量出的 `tools/` 缺口，按 Day 8 的两份方案分两块。对照时请先分清"这条缺口属于方案 A 还是方案 B"——这跟你 Day 8 报告里 #3（方案A）/ #4（方案B）是一一对应的。

### 方案 A · 五步↔工具增强 缺口（对应你报告 #3）

| 第三课步骤 | 当前对应（指南视角） | 状态 |
|---|---|---|
| ① 准备（闲钱/选宽基/看费率） | `config.py` 设 ETF/金额 ✓；费率未展示 | ⚠️ |
| ② 建仓（定投日·弹性股数法·记账） | 公式一致 ✓；记账 ✓；**滚存没真滚** ⚠️ | ⚠️ |
| ③ 持有（不盯盘·看PE/成本曲线·回测） | PE能查 ✓；**成本曲线缺** ❌；**回测未集成** ⚠️ | ⚠️ |
| ↳ 散落资产（应归进 ③ 持有的能力） | `Day5/code/backtest_3years.py`、`plot_returns.py`；`Day6/code/profit_taker_backtest.py` 散落在 Day5/Day6 的 `code/` 目录 | ⚠️ |
| ④ 止盈（25%卖半→再涨10%卖**剩余一半**→+30%回撤10%清余、转货基等低估） | **只有两档、缺第三档移动止盈**；**第二档算错会清仓**；**未接主菜单** | ✗ |

> 💡 「散落资产」一行归方案 A 的 ③ 持有步（让持有步不止"能查 PE"，还能"看曲线/跑回测"）。其处理方式已由指南定好边界（**物理文件不搬、Day5 的 `data/` CSV 不复制，只把能力 import/封装进 `tools/`**），不是开放题——你直接照此执行，把精力留给修复顺序、函数签名、自测场景这些真正要你判断的地方。

### 方案 B · 自动化增强 缺口（对应你报告 #4）

| 第三课步骤 | 当前对应（指南视角） | 状态 |
|---|---|---|
| ⑤ 纪律（不止损·三件事·浮亏20%哨点·**定时提醒 + 周期巡检**） | 日历是静态表 ✓；**哨点/提醒全缺** ❌；**自动巡检（把纪律交给 AI 定时看）也缺失** ❌：Day7 的 Cron 只推"该定投了"，从不检查止盈/预警，`profit_taker` 需手动 `python` 才跑，不跑工具就永远不知道该卖/该警惕 | ✗ |

> 方案 B 的本质：把方案 A 里 ④ 止盈、⑤ 哨点、PE 阈值这些"能力"，封装成可定时调用的纯函数 + 每周巡检 Cron——让纪律不再依赖你手动跑。先有方案 A 的能力，再做方案 B 的"定时调用外壳"。

**核对建议**：在你的 Day 8 报告里写一张「自查 vs 参考」对照——哪些一致、哪些我补了你没发现（你的盲区）、哪些你发现了我没列（你的独立发现）。**差异本身才是学习点**，不是谁漏了。两份方案各写一张，别混。

---

## 第二部分·参考改法（动手写代码前再打开）

> **今天只产出方案，不写代码。** 本文件今天只给你「缺口核对表」（上面第一部分）。具体的参考改法、状态机怎么写、自测数字、重置机制，放在另一份核对材料里，你**动手写代码前**再打开对照。
>
> 这样保留今天的「先自己想、再对答案」节奏：你用 AI 工具自查 + 对照本表第一部分，已经综合出了自己的修复方案；动手落地时，才用那份改法材料做最后核对。
>
> 👉 参考改法放在一份**独立的核对材料**里，你动手写代码前再由 Oliver / 你的协作助手 把它打开给你对照——今天不要提前看。
