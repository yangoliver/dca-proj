# Day 8 参考指南 — 从实战体感回到理论，用第三课补齐定投工具

> **核心目标**：前 7 天你已经在真实 A 股里买过、记过、写过止盈脚本、封装了 Skill、接了 Cron——有了体感。今天回到理论：重读《第三课·如何定投一只宽基指数基金》，把"定投到底该怎么走一遍"梳理清楚，再用这套方法作尺子，量一量你写的 `tools/` 差在哪、把缺口补齐（代码产出必须落在 `tools/` 目录）。下次定投实操之前，确保"全生命周期都有工具支持"。
>
> 📖 第三课原文：[如何定投一只宽基指数基金](../reading/第三课-如何定投一只宽基指数基金.md)

---

## 一、今天任务清单

### 必做任务（今天必须完成）

| # | 任务 | 完成标准 |
|---|------|---------|
| 1 | **回顾前 7 天体感** | 能说出"我实际踩过哪些坑、对哪条纪律最有感触" |
| 2 | **重读第三课，梳理五步方法** | 画出定投全生命周期五步，每步明确"该做什么、工具该帮什么" |
| 3 | **用第三课量 tools/ 缺口** | 先用 AI 工具自查 → 再对照指南参考表 → 综合成**自己的**修复方案（允许采纳所有建议） |
| 4 | **修止盈第三档（移动止盈）** | `profit_taker.py` 新增"+30%回撤10%→卖剩余全部、本轮结束"逻辑 |
| 5 | **修正第二档清仓 bug** | 第二档按"剩余持仓"计算，最终实现 50%/25%/25%，保留约 25% 底仓 |
| 6 | **把止盈接进主菜单** | `main.py` 新增"判止盈"选项，调用 `profit_taker.check()` 并展示建议 |
| 7 | **补浮亏 20% 预警哨点** | 新增逻辑：浮亏达 20% 时输出"预警哨点·不卖出·核对三件事"提示 |
| 8 | **修滚存真滚入下期** | `main.py` 累计份额/金额时并入上期余数，不再 `int()` 截断丢弃 |
| 9 | **设计止盈/预警自动巡检** | 把"判止盈+预警哨点"封装成可被定时任务调用的纯函数；设计一份 QClaw Cron 提示词，让 AI 定期跑工具并推送结论 |
| 10 | **Day 8 报告 + PR** | 报告提交 GitHub，PR 包含 `tools/` 下的全部改动 |

### 扩展任务（加分）

| # | 任务 | 完成标准 |
|---|------|---------|
| 10 | **PE 阈值提醒（不触发卖出）** | `portfolio.py` 查询时判断 PE>70% 打印"仅提醒"、PE<30% 打印"可分批加回" |
| 11 | **成本曲线图（最低要求）** | 用 `matplotlib`/ReportLab 生成成本 vs 市值的 PNG，不在主流程强制但要有脚本 |

---

## 二、第一课：为什么 Day8 是"回归理论日"？

前 7 天你是一边学一边干：

- Day 3 真在券商 APP 买了第一笔，知道了"手数""手续费"不是书上的词，是真金白银的约束；
- Day 4 写了弹性股数法，发现 500 块凑不出整数手，余钱得滚存；
- Day 6 写了止盈脚本，觉得自己懂了"涨了怎么卖"；
- Day 7 把工具封装成 Skill、接上 Cron，工具链成型了。

**但你有没有这种感觉**：每天在解决眼前的具体问题，却没机会停下来问一句——"我这套东西，到底是不是一条完整的定投？有没有哪一步我根本没覆盖到？"

这正是 Day 8 要做的事。**先有体感，再回到理论，理论的每一句话你才会"啊，我那天就是栽在这"**。第三课就是那个理论总纲——它把"如何定投一只宽基指数基金"讲全了五步。而你的工具，是比第三课早出生的，所以一定有代差。

> **Day8 的纪律**：不写新功能炫技，只做一件事——让 `tools/` 的每个文件，都能在第三课的五步里找到自己的位置，且行为一致。

---

## 三、第二课：重读第三课，梳理五步方法论

动手改代码前，先回到理论。打开 📖 [第三课·如何定投一只宽基指数基金](../reading/第三课-如何定投一只宽基指数基金.md)，带着你前 7 天的体感重读一遍，然后把下面的「五步」抄成你自己的清单——**每一步都要写清楚：这一步在干什么？工具应该帮我干什么？**

| 第三课步骤 | 这一步在干什么 | 工具应该帮我干什么 |
|---|---|---|
| ① 准备 | 确认是闲钱、选宽基、看费率 | `config.py` 设 ETF/金额；**应展示费率对比** |
| ② 建仓 | 定投日按弹性股数法买入、记账 | `calculator.py`算手数 + `recorder.py`记账 + `main`选项1 |
| ③ 持有 | 不盯盘；看 PE 分位、看成本曲线、偶尔回测 | `portfolio.py`查PE + 成本曲线 + 回测 |
| ④ 止盈 | 25%卖半→再涨10%卖余→转货基等低估 | `profit_taker.py` |
| ⑤ 纪律 | 不止损；浮亏20%只看三件事；定时提醒 | `scheduler.py`日历 + 预警逻辑 |

把这张表填进你的 Day 8 报告——这就是你今天工作的"尺子"。

---

## 四、第三课：用第三课这把尺，量 tools/ 的缺口

这一节**不替你下结论**。先让你用前 7 天搭好的 AI 工具自己把 `tools/` 量一遍，再把你的发现和下面这份「指南参考表」核对，最后综合成**你自己的**修复方案。

---

### Step A：先用 AI 工具自查（不要直接看下面的表）

打开 QClaw，把前 7 天封装好的 Skill 用起来，让它帮你通读 `tools/`，对照你刚梳理的第三课五步，回答这个问题：

> 「按我现在写的工具，第三课说的每一步，到底能不能跑通？哪一步是假的、哪一步会出 bug？」

可以这么问 QClaw：
- "读一遍 `tools/`，对照定投五步（准备/建仓/持有/止盈/纪律），哪些步骤工具其实覆盖不了？"
- "`profit_taker.py` 现在能正确实现第三课的止盈规则吗？逐档算给我看。"
- "如果我不每天手动跑工具，我怎么知道该止盈了？现在的工具链里有没有自动检查这一环？"

把 QClaw 的发现，**写进你的 Day 8 报告「我自查发现的缺口」**，这是你今天的第一份产出。

### Step B：再对照「指南参考表」（这是我的发现，不是你的作业）

下面这份表是**我**量出来的结果，给你对照用的——看我们想到了一块没有。

| 第三课步骤 | 工具应该做什么 | 当前对应（指南视角） | 状态 |
|---|---|---|---|
| ① 准备（闲钱/选宽基/看费率） | `config.py` 设 ETF/金额；**应展示费率对比** | `config.py` 设 ETF/金额 ✓；费率未展示 | ⚠️ |
| ② 建仓（定投日·弹性股数法·记账） | `calculator.py`算手数 + `recorder.py`记账 + `main`选项1 | 公式一致 ✓；记账 ✓；**滚存没真滚** ⚠️ | ⚠️ |
| ③ 持有（不盯盘·看PE/成本曲线·回测） | `portfolio.py`查PE + 成本曲线 + 回测 | PE能查 ✓；**成本曲线缺** ❌；**回测未集成** ⚠️ | ⚠️ |
| ④ 止盈（25%卖半→再涨10%卖余→转货基等低估） | `profit_taker.py` | **只有两档、缺第三档移动止盈**；**第二档算错会清仓**；**未接主菜单** | ✗ |
| ⑤ 纪律（不止损·三件事·浮亏20%哨点·**定时提醒**） | `scheduler.py`日历 + 预警逻辑 + **周期巡检自动化** | 日历是静态表 ✓；**哨点/提醒全缺** ❌；**更关键：没有"自动巡检"——Cron 只推"该定投了"，从不检查止盈/预警，不跑工具就永远不知道该卖** ❌ | ✗ |
| ⑥ **周期巡检（把纪律交给 AI 定时看）** | 定时（如每周）自动跑 `profit_taker.check()` + 浮亏预警 + PE 阈值，把结论推给杨知行 | **完全缺失** ❌：Day7 的 Cron 只提醒定投日，不检查止盈/预警；`profit_taker` 需手动 `python` 才跑 | ✗ |

### Step C：综合成你自己的修复方案

把 Step A（你的发现）和 Step B（我的发现）摆在一起，**允许你综合采纳所有建议，也允许你提出我们俩都没想到的改法**。然后做两件事：

1. 在你的 Day 8 报告里写一张「最终缺口清单」——哪些和你自查一致、哪些是我补的、你认不认、按什么顺序修；
2. 按这张清单动手改 `tools/`，代码必须落在 `tools/` 目录。

---

> **为什么自动化巡检一定在清单里？** 第三课的纪律（止盈、不止损、预警哨点）都依赖"你定期去看一眼"。但人不会每天盯盘——Day3 就学过"盘中不看盘"。如果工具只能手动 `python profit_taker.py <价>` 才跑，那浮盈到了 25%、或浮亏到了 20%，你根本不知道。Day7 你搭好了 Skill + 双周 Cron，但那个 Cron **只推"今天定投日"，从不检查止盈/预警**。所以今天必须把"判止盈 + 预警哨点 + PE 阈值"做成**可定时调用的纯函数**，再设计一份 QClaw Cron 提示词，让 AI 每周自动跑工具、把结论推到你微信。这正是第三课第五步"定时提醒"的工具落地。

### 🔴 指南参考思路（高严重度·供你采纳）

下面的具体改法只是**我的参考实现思路**，不是要你照抄的作业。你可以采纳、可以改进、可以用完全不同的写法——只要最终结果对齐第三课口径。

**1. 止盈只有两档，缺第三档（移动止盈+本轮结束）**
- 第三课第四步：「浮盈 +30% 后，从最高点回撤 10% → 卖剩余全部、本轮结束」。
- `profit_taker.py` 的现状：`state==2` 只是干等浮盈跌破 25% 再重置，**永远不会真正卖完、也不会标记本轮结束**。
- 参考改法：在 `check()` 里新增第三档——记录历史最高浮盈，当浮盈达 +30% 后从最高点回撤 10% 时，建议卖出剩余全部持仓，并标记 `state=3`（本轮结束）。

**2. 第二档卖出量算错，会清仓**
- 两档都按 `shares/2` 算，而 `shares` 取自 `portfolio.xlsx` 里**从未扣减卖出的总持仓**。
- 结果：第一档卖 50%、第二档又卖 50%（总计 100%）→ **直接清仓**，与第三课「不清仓、保留约 25%」正面冲突。
- 参考改法：卖出必须用"剩余持仓"为基数。第一档卖剩余 50%（=原仓 50%），第二档卖剩余 50%（=原仓 25%），第三档卖剩余 100%（=原仓 25%）。`record_sell` 同时要把卖出份额**扣减回 `portfolio.xlsx`**（或单独维护"当前可卖持仓"字段）。

**3. 止盈模块没接进主菜单**
- `main.py` 菜单只有 1执行/2持仓/3记录/4PE/5日历，**没有"判止盈"入口**。
- 第三课明确工具职责包含"判止盈"，但 `profit_taker.py` 是孤儿脚本，得手动 `python profit_taker.py <价>` 才跑得起来。
- 参考改法：`main.py` 新增选项（如"6. 判止盈"），读当前价 → 调 `profit_taker.check()` → 打印建议操作。

### 🟡 指南参考思路（中严重度·供你采纳）

**4. 滚存没真滚入下期**
- `calculator` 算出 `remainder` 只写进备注文本；`main.py` 用 `int()` 截断累计份额，下期仍按固定 `DCA_AMOUNT` 买，零头被丢弃。
- 参考改法：维护一个"累计滚存"变量（可放 `config` 或 `portfolio.xlsx` 某字段），每期买入金额 = `DCA_AMOUNT + 上期滚存`，买完把新余数再存回。

**5. 浮亏 20% 预警哨点缺失**
- 第三课把"浮亏 20% 是哨点不是卖出信号、只看三件事（闲钱/现金流/纪律）"作为纪律核心。
- `tools/` 完全没有对应逻辑。
- 参考改法：在 `main.py` 的"查看持仓"或新增"判纪律"里，当浮亏 ≥ 20% 时输出：「⚠️ 预警哨点：不卖出、不停止定投。请核对：①这笔钱还是闲钱吗？②后续现金还在吗？③纪律还在吗？」

**6. PE 只展示、无阈值提醒**
- 第三课要求「PE>70% 仅提醒不卖出」「PE<30% 分批加回下一轮」。
- `portfolio.py` 只拉分位打印，无阈值/决策逻辑。
- 参考改法（扩展任务 10）：`get_pe_data()` 返回分位后，加判断：>70% 打印"估值偏高·仅提醒·不触发卖出"；<30% 打印"估值偏低·可分批加回下一轮"。

### 🟢 指南参考思路（低严重度·有空再做）

- 成本曲线图未实现（仅数字）——扩展任务 11
- 费率未展示——扩展任务 11 顺带

> **自动巡检与本日任务的关系**：第 9 项（设计自动巡检）今天先完成"可调用纯函数 + Cron 提示词设计"；真正的 QClaw Cron 注册可在 Day8 之后由你或 Oliver 落地，但**设计必须今天做**，否则止盈纪律仍悬空。

---

## 五、第四课：动手前先对齐第三课原文

改代码前，把第三课里这几段**原话**读一遍，确保你改出来的行为和文章一字不差（带着 Day 3/Day 6 的体感读，你会更有感觉）：

- **第四步「纪律止盈」的止盈规则表**：单轮浮盈≥25%→卖当前持仓一半（第一档，约占总仓位50%）；第一档后价格再涨10%（相对第一档卖出价）→卖剩余一半（第二档，累计约75%，保留约25%）；移动止盈浮盈+30%后从最高点回撤10%→卖剩余全部（本轮结束）；PE>70%仅提醒不触发。
- **「跌了怎么办：不止损只看三件事」**：没有价格止损，只有资金管理止损；浮亏20%是预警哨点不是卖出信号。

你的代码改完，应该能用 `main.py` 走完：建仓 → 查持仓（含预警哨点）→ 判止盈（三档）→ 记录卖出（扣减持仓）。

---

## 六、第五课：Git 协作与 Pull Request

和 Day7 一样，今天改动走 PR。

### 6.1 这次 PR 提交什么
- `tools/profit_taker.py` — 补第三档 + 修正第二档基数
- `tools/main.py` — 新增"判止盈"菜单 + 修滚存 + 浮亏预警
- `tools/portfolio.py` — （若做扩展）PE 阈值提醒
- `tools/config.py` — 已是事实标准（单期 ¥500、共 20 期、双周），无需改动

### 6.2 PR 描述写什么
用一句话说明 Day8 目标："从实战体感回到第三课理论，以五步方法论为尺子，补齐 tools/ 与方法的缺口"。然后列改动清单 + 自测结果（尤其第二档不再清仓、第三档能触发本轮结束）。

---

## Day 8 交付

### 做了什么
- 回顾前 7 天体感，重读第三课，画出五步方法论
- 用第三课五步量 tools/，填完工具对齐盘点表，定位 🔴/🟡/🟢 缺口
- 修复止盈三档 + 清仓 bug + 接主菜单 + 滚存 + 浮亏预警

### 验证结果
- [ ] `python profit_taker.py <价>` 三档均能正确触发，第二档后持仓剩约 25%
- [ ] `main.py` 新增"判止盈"可运行，输出与第三课规则表一致
- [ ] 浮亏 ≥20% 时输出预警哨点话术（不卖出）
- [ ] 滚存余数并入下期，不再被 `int()` 截断

### 需要讨论
- 投资参数以 `config.py` 为事实标准（单期 ¥500、共 20 期、双周），任务书 v7.9 / 大富翁版 v2.5 已同步，无需在指南中作为任务。

---

## 七、给杨知行的提示

### 7.1 遇到困难时问 QClaw
"帮我看 profit_taker.py 的 check() 怎么加第三档"、"第二档为什么清仓，怎么改"。

### 7.2 15 分钟原则
卡住 15 分钟 → 问 QClaw → 问 Oliver。按这个顺序。

### 7.3 不要照抄
理解第三课的规则，自己写逻辑。能解释的改动，才是你真正学会的——这一课本来就是从体感回到理解。

---

## 八、验收清单

- [ ] 前 7 天体感已回顾，写进报告
- [ ] 第三课已重读，五步方法论清单已画，每步对应工具明确
- [ ] 工具对齐盘点表已填（✅/⚠️/❌）
- [ ] 🔴 三项高严重度全部修复并自测
- [ ] 🟡 滚存 + 浮亏预警已修复（PE 提醒为加分）
- [ ] 所有改动在 `tools/` 目录内
- [ ] Day 8 报告已提交，PR 已创建
