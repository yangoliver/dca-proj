# Day 8 参考指南 — 用第三课复盘全周期，把工具补齐

> **核心目标**：以《第三课·如何定投一只宽基指数基金》的五步生命周期为"验收标准"，盘点 `tools/` 当前实现与方法的缺口，并在 Day8 把缺失的工具补齐（代码产出必须落在 `tools/` 目录）。下次定投实操之前，确保"全生命周期都有工具支持"。

---

## 一、今天任务清单

### 必做任务（今天必须完成）

| # | 任务 | 完成标准 |
|---|------|---------|
| 1 | **重读第三课，画出五步清单** | 能列出五步 + 每一步对应的工具应该做什么 |
| 2 | **盘点 tools/ 与第三课的对齐度** | 填完下面的「工具对齐盘点表」，标出每个场景 ✅/⚠️/❌ |
| 3 | **修止盈第三档（移动止盈）** | `profit_taker.py` 新增"+30%回撤10%→卖剩余全部、本轮结束"逻辑 |
| 4 | **修正第二档清仓 bug** | 第二档按"剩余持仓"计算，最终实现 50%/25%/25%，保留约 25% 底仓 |
| 5 | **把止盈接进主菜单** | `main.py` 新增"判止盈"选项，调用 `profit_taker.check()` 并展示建议 |
| 6 | **补浮亏 20% 预警哨点** | 新增逻辑：浮亏达 20% 时输出"预警哨点·不卖出·核对三件事"提示 |
| 7 | **修滚存真滚入下期** | `main.py` 累计份额/金额时并入上期余数，不再 `int()` 截断丢弃 |
| 8 | **Day 8 报告 + PR** | 报告提交 GitHub，PR 包含 `tools/` 下的全部改动 |

### 扩展任务（加分）

| # | 任务 | 完成标准 |
|---|------|---------|
| 9 | **PE 阈值提醒（不触发卖出）** | `portfolio.py` 查询时判断 PE>70% 打印"仅提醒"、PE<30% 打印"可分批加回" |
| 10 | **成本曲线图（最低要求）** | 用 `matplotlib`/ReportLab 生成成本 vs 市值的 PNG，不在主流程强制但要有脚本 |
| 11 | **统一 config 参数** | 与任务书 v7.8 对齐：`DCA_AMOUNT` 按实际（¥1000 or ¥500 择一确认）、`TOTAL_COUNT=10` |

---

## 二、第一课：为什么 Day8 是"对齐日"？

前 7 天你造了工具、写了止盈脚本、封装了 Skill、接了 Cron。但第三课（文章）是**更晚写的总纲**——它把"如何定投一只宽基指数基金"讲全了五步。

问题来了：**你写的工具，跟文章讲的方法，对得上吗？**

答案是——大体对得上，但"止盈"这一步严重对不上，还有几个实打实的 bug。这不是你错了，是**工具比文章早出生**：`Day4 设计案`只规划到"弹性股数法+记录+分析"，止盈/PE/预警是后面补的，而文章把完整五步讲全了，工具没追上文章。

Day8 就是把这个"代差"补平。

> **Day8 的纪律**：不写新功能炫技，只做一件事——让 `tools/` 的每个文件，都能在第三课的五步里找到自己的位置，且行为一致。

---

## 三、第二课：第三课五步 ↔ 工具对齐盘点

先把第三课的方法论拆成"工具该做什么"，再逐项核对 `tools/` 现状。下面的表是你 Day8 的**工作底稿**，填完它，你就知道该改哪几个文件。

| 第三课步骤 | 工具应该做什么 | 当前对应 | 状态 |
|---|---|---|---|
| ① 准备（闲钱/选宽基/看费率） | `config.py` 设 ETF/金额；**应展示费率对比** | `config.py` 设 ETF/金额 ✓；费率未展示 | ⚠️ |
| ② 建仓（定投日·弹性股数法·记账） | `calculator.py`算手数 + `recorder.py`记账 + `main`选项1 | 公式一致 ✓；记账 ✓；**滚存没真滚** ⚠️ | ⚠️ |
| ③ 持有（不盯盘·看PE/成本曲线·回测） | `portfolio.py`查PE + 成本曲线 + 回测 | PE能查 ✓；**成本曲线缺** ❌；**回测未集成** ⚠️ | ⚠️ |
| ④ 止盈（25%卖半→再涨10%卖余→转货基等低估） | `profit_taker.py` | **只有两档、缺第三档移动止盈**；**第二档算错会清仓**；**未接主菜单** | ✗ |
| ⑤ 纪律（不止损·三件事·浮亏20%哨点·定时提醒） | `scheduler.py`日历 + 预警逻辑 | 日历是静态表 ✓；**哨点/提醒全缺** ❌ | ✗ |

### 🔴 高严重度（今天必须修）

**1. 止盈只有两档，缺第三档（移动止盈+本轮结束）**
- 第三课第三步：「浮盈 +30% 后，从最高点回撤 10% → 卖剩余全部、本轮结束」。
- `profit_taker.py` 的现状：`state==2` 只是干等浮盈跌破 25% 再重置，**永远不会真正卖完、也不会标记本轮结束**。
- **改法**：在 `check()` 里新增第三档——记录历史最高浮盈，当浮盈达 +30% 后从最高点回撤 10% 时，建议卖出剩余全部持仓，并标记 `state=3`（本轮结束）。

**2. 第二档卖出量算错，会清仓**
- 两档都按 `shares/2` 算，而 `shares` 取自 `portfolio.xlsx` 里**从未扣减卖出的总持仓**。
- 结果：第一档卖 50%、第二档又卖 50%（总计 100%）→ **直接清仓**，与第三课「不清仓、保留约 25%」正面冲突。
- **改法**：卖出必须用"剩余持仓"为基数。第一档卖剩余 50%（=原仓 50%），第二档卖剩余 50%（=原仓 25%），第三档卖剩余 100%（=原仓 25%）。`record_sell` 同时要把卖出份额**扣减回 `portfolio.xlsx`**（或单独维护"当前可卖持仓"字段）。

**3. 止盈模块没接进主菜单**
- `main.py` 菜单只有 1执行/2持仓/3记录/4PE/5日历，**没有"判止盈"入口**。
- 第三课明确工具职责包含"判止盈"，但 `profit_taker.py` 是孤儿脚本，得手动 `python profit_taker.py <价>` 才跑得起来。
- **改法**：`main.py` 新增选项（如"6. 判止盈"），读当前价 → 调 `profit_taker.check()` → 打印建议操作。

### 🟡 中严重度（今天尽量修）

**4. 滚存没真滚入下期**
- `calculator` 算出 `remainder` 只写进备注文本；`main.py` 用 `int()` 截断累计份额，下期仍按固定 `DCA_AMOUNT` 买，零头被丢弃。
- **改法**：维护一个"累计滚存"变量（可放 `config` 或 `portfolio.xlsx` 某字段），每期买入金额 = `DCA_AMOUNT + 上期滚存`，买完把新余数再存回。

**5. 浮亏 20% 预警哨点缺失**
- 第三课把"浮亏 20% 是哨点不是卖出信号、只看三件事（闲钱/现金流/纪律）"作为纪律核心。
- `tools/` 完全没有对应逻辑。
- **改法**：在 `main.py` 的"查看持仓"或新增"判纪律"里，当浮亏 ≥ 20% 时输出：「⚠️ 预警哨点：不卖出、不停止定投。请核对：①这笔钱还是闲钱吗？②后续现金还在吗？③纪律还在吗？」

**6. PE 只展示、无阈值提醒**
- 第三课要求「PE>70% 仅提醒不卖出」「PE<30% 分批加回下一轮」。
- `portfolio.py` 只拉分位打印，无阈值/决策逻辑。
- **改法（扩展任务 9）**：`get_pe_data()` 返回分位后，加判断：>70% 打印"估值偏高·仅提醒·不触发卖出"；<30% 打印"估值偏低·可分批加回下一轮"。

### 🟢 低严重度（有空再做）

- 定时推送未实现（`scheduler.py` 只是 Excel 静态日历，无微信/邮件推送——等 Cron 成熟再说）
- 成本曲线图未实现（仅数字）——扩展任务 10
- 费率未展示——扩展任务 11 顺带

---

## 四、第三课：动手前先对齐第三课原文

改代码前，把第三课里这几段**原话**读一遍，确保你改出来的行为和文章一字不差：

- **第五步「何时卖与纪律止盈」的止盈规则表**：单轮浮盈≥25%→卖当前持仓一半（第一档，约占总仓位50%）；第一档后价格再涨10%（相对第一档卖出价）→卖剩余一半（第二档，累计约75%，保留约25%）；移动止盈浮盈+30%后从最高点回撤10%→卖剩余全部（本轮结束）；PE>70%仅提醒不触发。
- **「跌了怎么办：不止损只看三件事」**：没有价格止损，只有资金管理止损；浮亏20%是预警哨点不是卖出信号。

你的代码改完，应该能用 `main.py` 走完：建仓 → 查持仓（含预警哨点）→ 判止盈（三档）→ 记录卖出（扣减持仓）。

---

## 五、第四课：Git 协作与 Pull Request

和 Day7 一样，今天改动走 PR。

### 5.1 这次 PR 提交什么
- `tools/profit_taker.py` — 补第三档 + 修正第二档基数
- `tools/main.py` — 新增"判止盈"菜单 + 修滚存 + 浮亏预警
- `tools/portfolio.py` — （若做扩展）PE 阈值提醒
- `tools/config.py` — （若做扩展）参数对齐

### 5.2 PR 描述写什么
用一句话说明 Day8 目标："以第三课五步为验收标准，补齐 tools/ 与方法的缺口"。然后列改动清单 + 自测结果（尤其第二档不再清仓、第三档能触发本轮结束）。

---

## Day 8 交付

### 做了什么
- 重读第三课，画出五步方法论
- 填完工具对齐盘点表，定位 🔴/🟡/🟢 缺口
- 修复止盈三档 + 清仓 bug + 接主菜单 + 滚存 + 浮亏预警

### 验证结果
- [ ] `python profit_taker.py <价>` 三档均能正确触发，第二档后持仓剩约 25%
- [ ] `main.py` 新增"判止盈"可运行，输出与第三课规则表一致
- [ ] 浮亏 ≥20% 时输出预警哨点话术（不卖出）
- [ ] 滚存余数并入下期，不再被 `int()` 截断

### 需要讨论
- `DCA_AMOUNT` / `TOTAL_COUNT` 与任务书 v7.8 的参数矛盾（500/1000、20/10）何时统一？

---

## 六、给杨知行的提示

### 6.1 遇到困难时问 QClaw
"帮我看 profit_taker.py 的 check() 怎么加第三档"、"第二档为什么清仓，怎么改"。

### 6.2 15 分钟原则
卡住 15 分钟 → 问 QClaw → 问 Oliver。按这个顺序。

### 6.3 不要照抄
理解第三课的规则，自己写逻辑。能解释的改动，才是你真正学会的。

---

## 七、验收清单

- [ ] 第三课五步清单已画，每步对应工具明确
- [ ] 工具对齐盘点表已填（✅/⚠️/❌）
- [ ] 🔴 三项高严重度全部修复并自测
- [ ] 🟡 滚存 + 浮亏预警已修复（PE 提醒为加分）
- [ ] 所有改动在 `tools/` 目录内
- [ ] Day 8 报告已提交，PR 已创建
